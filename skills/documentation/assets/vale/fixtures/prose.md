# Documentation checks

We use the cache.

Use DoxyGen and Mkdocs.

Doxygen, MkDocs, Vale, lychee, and Diataxis describe this workflow.

Use `DoxyGen` with [the guide](https://example.com/Mkdocs).

```cpp
const char *message = "Simply utilize DoxyGen";
```

<!-- vale Google.We = NO -->
We use the quoted product label.
<!-- vale Google.We = YES -->

We inspect the result.
