# Count text lines

The default input encoding is ASCII.

Read [input formats](formats-old.md).

For an empty file, the command prints `1`.

The processor accepts the input path.

The parent guide says that empty files produce an error.

The design eliminates all possible data loss.

The command reads one local input file.
