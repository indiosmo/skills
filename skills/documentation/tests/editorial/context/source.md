# Counter source contract

The checked-in Text Workshop `evidence/count_lines.sh` accepts one readable
regular file. Its samples produce counts 2, 0 and 1 for complete, empty and
unterminated input. Use the term `counter`. Bash and wc are prerequisites.
The script requires the input path; it supplies no interactive setup wizard.
