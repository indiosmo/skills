# Easily Count Any File

The counter supports all file formats at unlimited speed.

1. Run the script.
2. Configure the processor appropriately.
3. Deploy the remote service with the standard production command.

The processor reads the text file. It is recommended that the file be counted.

## Understanding the philosophy of text processing

A complete history of text encoding explains why line-oriented interfaces became
popular. Compare competing theories of stream processing before proceeding with
the next operational step.

```sh
bash tools/count-lines.sh "samples/two-lines.txt --verbose"
```
